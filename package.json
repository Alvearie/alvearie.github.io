{
  "name": "example",
  "private": true,
  "version": "1.27.0",
  "license": "Apache 2.0",
  "scripts": {
    "dev": "gatsby develop -H 0.0.0.0",
    "dev:clean": "yarn clean && yarn dev",
    "build": "gatsby build && xcopy \"public\" \"docs\" /s /i /y /q",
    "copyOutput": "xcopy \"public\" \"docs\" /s /i /y /q",
    "ghpages": "gh-pages -d public",
    "build:clean": "yarn clean && gatsby build",
    "serve": "gatsby serve",
    "clean": "gatsby clean"
  },
  "dependencies": {
    "gatsby": "^2.32.3",
    "gatsby-image": "^2.8.0",
    "gatsby-plugin-sharp": "^2.11.2",
    "gatsby-theme-carbon": "^1.27.1",
    "gatsby-transformer-sharp": "^2.9.0",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0",
    "rsync": "^0.6.1"
  }
}
